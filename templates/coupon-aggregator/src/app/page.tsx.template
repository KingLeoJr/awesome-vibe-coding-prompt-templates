import { motion } from "framer-motion";
import { ArrowRight, BadgePercent, ChevronRight, MapPin, Newspaper, Search, Sparkles } from "lucide-react";
import SEOHead from "@/components/shared/SEOHead";
import Header from "@/components/layout/Header";
import Footer from "@/components/shared/Footer";
import CouponBrowser from "@/components/coupons/CouponBrowser";
import ExpiringTimeline from "@/components/coupons/ExpiringTimeline";
import SavingsImpact from "@/components/coupons/SavingsImpact";
import data from "@/data/{{app.slug}}.json";

const sectionHeading = (id: string, title: string, subtitle: string) => (
  <div className="mx-auto mb-10 max-w-2xl text-center">
    <h2 id={id} className="text-3xl font-bold tracking-tight text-gray-900 dark:text-white sm:text-4xl">
      {title}
    </h2>
    <p className="mt-3 text-base text-gray-500 dark:text-gray-400">{subtitle}</p>
  </div>
);

export default function Home() {
  return (
    <>
      <SEOHead />
      <Header />
      <main>
        <section id="discover" className="relative overflow-hidden pb-20 pt-36">
          <div
            aria-hidden="true"
            className="pointer-events-none absolute inset-0 bg-[radial-gradient(ellipse_at_top,color-mix(in_srgb,var(--color-primary)_12%,transparent),transparent_60%)]"
          />
          <div className="container mx-auto px-6 text-center">
            <motion.p
              initial={{ opacity: 0, y: 12 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.5 }}
              className="mx-auto inline-flex items-center gap-2 rounded-full border border-[var(--color-primary)]/30 bg-[var(--color-primary)]/5 px-4 py-1.5 text-xs font-medium text-[var(--color-primary)]"
            >
              <Sparkles size={13} />
              {data.app.tagline}
            </motion.p>
            <motion.h1
              initial={{ opacity: 0, y: 16 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6, delay: 0.1 }}
              className="mx-auto mt-6 max-w-3xl text-4xl font-bold tracking-tight text-gray-900 dark:text-white sm:text-6xl"
            >
              Find the best deals,{" "}
              <span className="bg-gradient-to-r from-[var(--color-primary)] to-[var(--color-secondary)] bg-clip-text text-transparent">
                before they expire.
              </span>
            </motion.h1>
            <motion.p
              initial={{ opacity: 0, y: 16 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6, delay: 0.2 }}
              className="mx-auto mt-6 max-w-2xl text-lg text-gray-500 dark:text-gray-400"
            >
              Hand-picked, community-verified coupons from hundreds of retailers —
              searchable by category, store, or expiration date.
            </motion.p>
            <motion.form
              initial={{ opacity: 0, y: 16 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6, delay: 0.3 }}
              action="#coupons"
              className="mx-auto mt-9 flex max-w-xl items-stretch gap-2 rounded-full border border-gray-200 bg-white p-2 shadow-sm dark:border-gray-700 dark:bg-gray-950"
              role="search"
            >
              <span className="flex items-center pl-3 text-gray-400">
                <Search size={18} />
              </span>
              <input
                type="search"
                name="q"
                placeholder="Try “grocery”, “Amazon”, or “electronics”…"
                aria-label="Search deals"
                className="w-full bg-transparent text-sm text-gray-900 outline-none placeholder:text-gray-400 dark:text-white"
              />
              <button
                type="submit"
                className="inline-flex shrink-0 items-center gap-1.5 rounded-full bg-[var(--color-primary)] px-6 py-2.5 text-sm font-medium text-white transition hover:opacity-90"
              >
                Search deals <ArrowRight size={15} />
              </button>
            </motion.form>
            <motion.p
              initial={{ opacity: 0 }}
              animate={{ opacity: 1 }}
              transition={{ duration: 0.6, delay: 0.45 }}
              className="mt-4 flex items-center justify-center gap-1.5 text-xs text-gray-400"
            >
              <MapPin size={12} />
              1,200+ local stores near you with active promotions
            </motion.p>
          </div>
        </section>

        <section className="container mx-auto -mt-8 px-6">
          <div className="flex gap-3 overflow-x-auto pb-2" role="list" aria-label="Categories">
            {data.categories.map((category, index) => (
              <motion.a
                key={category.slug}
                href="#coupons"
                role="listitem"
                initial={{ opacity: 0, x: -12 }}
                whileInView={{ opacity: 1, x: 0 }}
                viewport={{ once: true }}
                transition={{ duration: 0.4, delay: index * 0.05 }}
                className="flex shrink-0 items-center gap-2 rounded-2xl border border-gray-200 bg-white px-5 py-3 text-sm font-medium text-gray-700 transition hover:border-[var(--color-primary)]/40 hover:text-[var(--color-primary)] dark:border-gray-700 dark:bg-gray-950 dark:text-gray-200"
              >
                <BadgePercent size={15} className="text-[var(--color-secondary)]" />
                {category.name}
                <span className="rounded-full bg-gray-100 px-1.5 text-[11px] font-semibold text-gray-500 dark:bg-gray-800 dark:text-gray-400">
                  {category.count}
                </span>
              </motion.a>
            ))}
          </div>
        </section>

        <section id="coupons" className="py-24">
          <div className="container mx-auto px-6">
            {sectionHeading("coupons", "Fresh deals, verified daily", "Every submission passes a human + automated verification before going live.")}
            <CouponBrowser coupons={data.coupons} categories={data.categories.map((c) => c.name)} />
          </div>
        </section>

        <section id="expiring" className="bg-gray-50 py-24 dark:bg-gray-900/40">
          <div className="container mx-auto px-6">
            {sectionHeading("expiring", "Don't miss the deadline", "Expiring-soon alerts and the seasonal discount calendar.")}
            <ExpiringTimeline expiring={data.expiring} trends={data.trends} />
          </div>
        </section>

        <section id="impact" className="py-24">
          <div className="container mx-auto px-6">
            {sectionHeading("impact", "What the community saves", "Real impact reporting on money saved, deals verified, and members rewarded.")}
            <SavingsImpact impact={data.impact} />
          </div>
        </section>

        <section id="faq" className="bg-gray-50 py-24 dark:bg-gray-900/40">
          <div className="container mx-auto max-w-3xl px-6">
            {sectionHeading("faq", "Common questions", "How the aggregator works, and how to redeem offers.")}
            <div className="space-y-3">
              {data.faqs.map((faq) => (
                <details
                  key={faq.question}
                  className="group rounded-2xl border border-gray-200 bg-white px-6 py-4 open:shadow-sm dark:border-gray-800 dark:bg-gray-950"
                >
                  <summary className="flex cursor-pointer list-none items-center justify-between gap-4 text-sm font-semibold text-gray-900 dark:text-white">
                    {faq.question}
                    <ChevronRight size={16} className="shrink-0 text-gray-400 transition-transform group-open:rotate-90" />
                  </summary>
                  <p className="mt-3 text-sm leading-relaxed text-gray-500 dark:text-gray-400">{faq.answer}</p>
                </details>
              ))}
            </div>
          </div>
        </section>

        <section id="blog" className="py-24">
          <div className="container mx-auto px-6">
            {sectionHeading("blog", "Saving tips & money news", "Practical advice for getting the most out of every coupon.")}
            <div className="grid gap-5 md:grid-cols-3">
              {data.blogPosts.map((post, index) => (
                <motion.article
                  key={post.slug}
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ duration: 0.45, delay: index * 0.07 }}
                  className="flex flex-col gap-3 rounded-2xl border border-gray-200 bg-white p-6 transition hover:border-[var(--color-primary)]/40 hover:shadow-md dark:border-gray-800 dark:bg-gray-950"
                >
                  <span className="inline-flex items-center gap-1.5 text-xs font-semibold text-[var(--color-secondary)]">
                    <Newspaper size={13} />
                    {post.tag}
                  </span>
                  <h3 className="font-semibold leading-snug text-gray-900 dark:text-white">{post.title}</h3>
                  <p className="line-clamp-3 text-sm text-gray-500 dark:text-gray-400">{post.excerpt}</p>
                  <p className="mt-auto flex items-center justify-between text-xs text-gray-400">
                    <span>{post.date}</span>
                    <span>{post.readMinutes} min read</span>
                  </p>
                </motion.article>
              ))}
            </div>
          </div>
        </section>

        <section className="pb-24">
          <div className="container mx-auto px-6">
            <div className="relative overflow-hidden rounded-3xl border border-[var(--color-primary)]/20 bg-gradient-to-br from-[var(--color-primary)] to-[var(--color-secondary)] px-8 py-14 text-center">
              <div className="relative z-10">
                <h2 className="text-3xl font-bold text-white sm:text-4xl">Found a deal we missed?</h2>
                <p className="mx-auto mt-4 max-w-xl text-sm text-white/80">
                  Submit it, earn loyalty points on verification, and unlock exclusive rewards.
                </p>
                <a
                  href="#coupons"
                  className="mt-8 inline-flex items-center gap-2 rounded-full bg-white px-7 py-3 text-sm font-medium text-[var(--color-primary)] transition hover:bg-gray-100"
                >
                  Submit a deal <ArrowRight size={16} />
                </a>
              </div>
            </div>
          </div>
        </section>
      </main>
      <Footer />
    </>
  );
}