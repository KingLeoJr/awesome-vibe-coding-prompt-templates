"use client";

import { motion } from "framer-motion";
import { cn } from "@/lib/utils";
import { Sun, Moon } from "lucide-react";

interface VirtualDestination {
  id: string;
  title: string;
  region: string;
  duration: number;
  price: number;
  imageUrl?: string;
}

interface VirtualTourProps {
  destination: VirtualDestination;
  onBook: (bookingId: string) => void;
}

export default function VirtualTour({ destination, onBook }: VirtualTourProps) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true }}
      transition={{ duration: 0.5, delay: 0.1 }}
      className="relative min-h-[400px] w-full rounded-2xl overflow-hidden dark:border-gray-800"
    >
      <motion.img
        src={destination.imageUrl || "/placeholder-vr-tour.jpg"}
        alt={destination.title} className="absolute inset-0 w-full h-full object-cover transition-opacity duration-500"
        transition={{ opacity: [0, 1] }}
      />
      <div className="absolute inset-0 bg-gradient-to-b from-black/40 via-transparent to-black/60 sm:from-black/30 dark:bg-black/90 sm:dark:bg-black/80" />
      <div className="absolute bottom-0 left-0 right-0 p-6">
        <div className="flex items-center justify-between">
          <div>
            <h2 className="font-bold text-2xl text-white">{destination.title}</h2>
            <p className="text-sm text-white/70">{destination.duration} min • ${destination.price}</p>
          </div>
          <button
            onClick={() => onBook("temp-" + destination.id)}
            className="px-4 py-2 rounded-full bg-white/20 text-white text-sm hover:bg-white/30 transition"
            aria-label="Book this tour"
          >
            Book Now
          </button>
        </div>
        <div className="absolute top-6 left-6 right-6">
          <div className="bg-white/10 dark:bg-gray-900/50 rounded-full h-2.5">
            <div
              className="h-full rounded-full bg-gradient-to-r from-[var(--color-primary)] to-[var(--color-secondary)] transform transition-transform"
              style={{ width: `${(destination.price / 500) * 100}%` }}
            />
          </div>
          <p className="text-xs text-white/60 mt-1 text-center">${destination.price}</p>
        </div>
      </div>
    </motion.div>
  );
}