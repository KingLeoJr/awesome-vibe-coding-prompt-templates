import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";
import AlertPanel from "@/components/home/AlertPanel";
import RoomSection from "@/components/home/RoomSection";
import SceneControls from "@/components/home/SceneControls";
import EnergyDashboard from "@/components/home/EnergyDashboard";
import data from "@/data/iot-smart-home.json";

export default function HomePage() {
  const { home } = data;
  const onlineRatio = Math.round((home.onlineCount / home.deviceCount) * 100);

  return (
    <div className="min-h-screen bg-gray-50 dark:bg-gray-950">
      <Header />

      <main>
        <section
          id="overview"
          className="bg-gradient-to-br from-[var(--color-primary)]/15 via-transparent to-[var(--color-secondary)]/15"
        >
          <div className="container mx-auto px-6 pt-28 pb-16 md:pt-36">
            <div className="flex flex-wrap items-end justify-between gap-6">
              <div>
                <p className="text-sm font-semibold text-[var(--color-primary)]">
                  {home.status} · {home.occupancy}
                </p>
                <h1 className="mt-2 text-3xl font-bold text-gray-900 dark:text-white sm:text-4xl">
                  {home.name}
                </h1>
                <p className="mt-3 max-w-xl text-gray-500 dark:text-gray-400">
                  {home.onlineCount} of {home.deviceCount} devices online ·{" "}
                  {home.temperature}°C · {home.humidity}% humidity
                </p>
              </div>

              <div className="w-full max-w-xs rounded-3xl border border-gray-200 dark:border-gray-800 bg-white/70 p-5 backdrop-blur dark:bg-gray-950/70">
                <div className="mb-2 flex items-center justify-between text-sm">
                  <span className="text-gray-500 dark:text-gray-400">Devices online</span>
                  <span className="font-bold text-gray-900 dark:text-white tabular-nums">
                    {home.onlineCount}/{home.deviceCount}
                  </span>
                </div>
                <div
                  className="h-2.5 w-full overflow-hidden rounded-full bg-gray-100 dark:bg-gray-800"
                  role="progressbar"
                  aria-label="Devices online"
                  aria-valuenow={home.onlineCount}
                  aria-valuemin={0}
                  aria-valuemax={home.deviceCount}
                >
                  <div
                    className="h-full rounded-full bg-gradient-to-r from-[var(--color-primary)] to-[var(--color-secondary)]"
                    style={{ width: `${onlineRatio}%` }}
                  />
                </div>
              </div>
            </div>

            <div className="mt-10">
              <AlertPanel alerts={data.alerts} />
            </div>
          </div>
        </section>

        <section id="rooms" className="container mx-auto scroll-mt-24 px-6 py-20">
          <div className="mb-10 max-w-2xl">
            <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Rooms</h2>
            <p className="mt-2 text-gray-500 dark:text-gray-400">
              Every room, every device — status and control at a glance.
            </p>
          </div>
          <div className="space-y-6">
            {data.rooms.map((room, index) => (
              <RoomSection key={room.id} room={room} delay={index * 0.06} />
            ))}
          </div>
        </section>

        <section id="scenes" className="bg-white dark:bg-gray-900 scroll-mt-24">
          <div className="container mx-auto px-6 py-20">
            <div className="mb-10 max-w-2xl">
              <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Scenes</h2>
              <p className="mt-2 text-gray-500 dark:text-gray-400">
                One tap to run a whole automation — orchestrated by the rules engine.
              </p>
            </div>
            <SceneControls scenes={data.scenes} />
          </div>
        </section>

        <section id="energy" className="container mx-auto scroll-mt-24 px-6 py-20">
          <div className="mb-10 max-w-2xl">
            <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Energy</h2>
            <p className="mt-2 text-gray-500 dark:text-gray-400">
              Consumption, cost, and solar production across the home.
            </p>
          </div>
          <EnergyDashboard energy={data.energy} />
        </section>
      </main>

      <Footer />
    </div>
  );
}