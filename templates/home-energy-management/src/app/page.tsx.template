import Header from "@/components/layout/Header";
import DeviceAutomation, { type EnergyDevice } from "@/components/energy/DeviceAutomation";
import EnergyDashboard, { type EnergyData } from "@/components/energy/EnergyDashboard";
import HomeAudit, { type AuditQuestion } from "@/components/energy/HomeAudit";
import ResourceLibrary, { type ResourceItem } from "@/components/energy/ResourceLibrary";
import SavingsRecommendations, { type SavingsRecommendation } from "@/components/energy/SavingsRecommendations";
import UtilityBillAnalyzer, { type UtilityBill } from "@/components/energy/UtilityBillAnalyzer";
import Footer from "@/components/shared/Footer";
import data from "@/data/{{app.slug}}.json";

export default function Home() {
  return (
    <>
      <Header />
      <main>
        <section className="bg-gradient-to-br from-[var(--color-primary)]/15 via-transparent to-[var(--color-secondary)]/15">
          <div className="mx-auto max-w-7xl px-6 pt-28 pb-16 text-center md:pt-36">
            <p className="text-sm font-semibold uppercase tracking-wider text-[var(--color-primary)]">
              {{app.tagline}}
            </p>
            <h1 className="mx-auto mt-4 max-w-3xl text-4xl font-extrabold tracking-tight text-gray-900 dark:text-white sm:text-5xl">
              {{hero.headline}}
            </h1>
            <p className="mx-auto mt-4 max-w-2xl text-lg text-gray-600 dark:text-gray-300">
              {{hero.subtext}}
            </p>
          </div>
        </section>

        <section id="overview" className="mx-auto max-w-7xl scroll-mt-24 px-6 py-16">
          <EnergyDashboard energy={data.energy as EnergyData} currency={data.currency as string} />
        </section>

        <section id="savings" className="scroll-mt-24 bg-gray-50 dark:bg-gray-950/40">
          <SavingsRecommendations
            recommendations={data.recommendations as SavingsRecommendation[]}
            currency={data.currency as string}
          />
        </section>

        <section id="bills" className="mx-auto max-w-7xl scroll-mt-24 px-6 py-16">
          <UtilityBillAnalyzer bills={data.bills as UtilityBill[]} currency={data.currency as string} />
        </section>

        <section id="devices" className="scroll-mt-24 bg-gray-50 dark:bg-gray-950/40">
          <DeviceAutomation devices={data.devices as EnergyDevice[]} />
        </section>

        <section id="audit" className="mx-auto max-w-7xl scroll-mt-24 px-6 py-16">
          <HomeAudit questions={data.auditQuestions as AuditQuestion[]} />
        </section>

        <section id="learn" className="scroll-mt-24 bg-gray-50 dark:bg-gray-950/40">
          <ResourceLibrary resources={data.resources as ResourceItem[]} />
        </section>
      </main>
      <Footer name={data.app.name as string} description={data.app.tagline as string} />
    </>
  );
}