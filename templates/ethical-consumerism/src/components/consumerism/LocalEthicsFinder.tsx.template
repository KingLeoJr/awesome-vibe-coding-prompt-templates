"use client";

import { motion } from "framer-motion";
import { MapPin, Navigation, Star, Store } from "lucide-react";

export interface LocalBusiness {
  id: string;
  name: string;
  category: string;
  distance: string;
  address: string;
  tags: string[];
  rating: number;
}

interface LocalEthicsFinderProps {
  businesses: LocalBusiness[];
}

export default function LocalEthicsFinder({ businesses }: LocalEthicsFinderProps) {
  return (
    <section aria-labelledby="local-heading">
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
        transition={{ duration: 0.5 }}
      >
        <div className="flex items-center gap-3 mb-8">
          <span className="flex h-11 w-11 items-center justify-center rounded-2xl bg-[var(--color-accent)]/10 text-[var(--color-accent)]">
            <Store size={22} aria-hidden="true" />
          </span>
          <div>
            <h2 id="local-heading" className="text-3xl font-bold text-gray-900 dark:text-white">Ethical businesses near you</h2>
            <p className="text-sm text-gray-500 dark:text-gray-400">Local shops and services that put people and planet first.</p>
          </div>
        </div>

        <div className="grid gap-5 md:grid-cols-2">
          {businesses.map((b, i) => (
            <motion.article
              key={b.id}
              initial={{ opacity: 0, y: 16 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.4, delay: i * 0.08 }}
              className="flex flex-col rounded-2xl border border-gray-200 dark:border-gray-800 bg-white dark:bg-gray-950 p-6"
            >
              <div className="flex items-start justify-between gap-4">
                <div>
                  <h3 className="font-semibold text-gray-900 dark:text-white">{b.name}</h3>
                  <p className="text-xs text-gray-500 dark:text-gray-400">{b.category}</p>
                </div>
                <span className="inline-flex items-center gap-1 rounded-full bg-[var(--color-accent)]/10 px-2.5 py-1 text-xs font-semibold text-[var(--color-accent)]">
                  <Star size={12} aria-hidden="true" />
                  {b.rating}
                </span>
              </div>

              <p className="mt-3 flex items-center gap-1.5 text-sm text-gray-600 dark:text-gray-300">
                <MapPin size={15} className="shrink-0 text-[var(--color-primary)]" aria-hidden="true" />
                {b.address}
              </p>

              <div className="mt-3 flex flex-wrap gap-2">
                {b.tags.map((tag) => (
                  <span key={tag} className="rounded-full bg-[var(--color-primary)]/10 px-2.5 py-1 text-xs font-medium text-[var(--color-primary)]">
                    {tag}
                  </span>
                ))}
              </div>

              <button className="mt-auto inline-flex w-fit items-center gap-1.5 pt-5 text-sm font-medium text-[var(--color-secondary)] hover:opacity-80 transition">
                <Navigation size={14} aria-hidden="true" />
                {b.distance} · Get directions
              </button>
            </motion.article>
          ))}
        </div>
      </motion.div>
    </section>
  );
}