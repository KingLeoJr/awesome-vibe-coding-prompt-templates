import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";
import data from "@/data/portfolio-website";

export default function HomePage() {
  const {
    header,
    hero,
    about,
    skills,
    projects,
    testimonials,
    experience,
    blog,
    contact,
  } = data;

  return (
    <div className="min-h-screen bg-gray-50 dark:bg-gray-950">
      <Header
        brandName="{{app.name}}"
        navItems={header.navItems}
        ctaText="{{header.ctaText}}"
        ctaHref="{{header.ctaHref}}"
        isFixed={{header.isFixed}}
        showMobileMenu={{header.showMobileMenu}}
        mobileNavItems={{header.mobileNavItems}}
      />

      <main>
        <section id="hero" className="bg-gradient-to-br from-[var(--color-primary)]/10 via-transparent to-[var(--color-secondary)]/10 pt-24 md:pt-32 pb-16">
          <div className="container mx-auto px-6">
            {hero.title && (
              <h1 className="text-4xl md:text-5xl font-bold text-gray-900 dark:text-white mb-4">
                {hero.title}
              </h1>
            )}
            {hero.subtitle && (
              <p className="text-lg md:text-xl text-gray-600 dark:text-gray-400 mb-8">
                {hero.subtitle}
              </p>
            )}
            {hero.ctaText && (
              <a href={hero.ctaHref} className="inline-block px-6 py-3 bg-[var(--color-accent)] text-white font-medium rounded hover:bg-[var(--color-accent-dark)] transition-colors">
                {hero.ctaText}
              </a>
            )}
          </div>
        </section>

        <section id="about" className="container mx-auto px-6 py-16">
          <div className="max-w-3xl">
            {about.headshot && (
              <img
                src={about.headshot}
                alt={about.bio}
                className="w-24 h-24 rounded-full object-cover mb-4 flex-shrink-0"
              />
            )}
            {about.bio && (
              <p className="text-gray-600 dark:text-gray-400 mb-6">
                {about.bio}
              </p>
            )}
          </div>
          <Skills
            progressBars={skills.progressBars}
            wordCloudData={skills.wordCloudData}
          />
        </section>

        <section id="projects" className="py-16 bg-white dark:bg-gray-900">
          <div className="container mx-auto px-6">
            <h2 className="text-3xl font-bold text-gray-900 dark:text-white mb-8">
              Projects
            </h2>
            <Projects projects={projects.projects} filterable={projects.filterable} masonry={projects.masonry} />
          </div>
        </section>

        <section id="testimonials" className="py-16 bg-gray-50 dark:bg-gray-950">
          <div className="container mx-auto px-6">
            <h2 className="text-3xl font-bold text-gray-900 dark:text-white mb-8">
              Testimonials
            </h2>
            <Testimonials testimonials={testimonials.testimonials} />
          </div>
        </section>

        <section id="experience" className="py-16 bg-white dark:bg-gray-900">
          <div className="container mx-auto px-6">
            <h2 className="text-3xl font-bold text-gray-900 dark:text-white mb-8">
              Experience
            </h2>
            <Experience experiences={experience.experiences} />
          </div>
        </section>

        <section id="blog" className="py-16 bg-gray-50 dark:bg-gray-950">
          <div className="container mx-auto px-6">
            <h2 className="text-3xl font-bold text-gray-900 dark:text-white mb-8">
              Blog
            </h2>
            <Blog posts={blog.posts} />
          </div>
        </section>

        <section id="contact" className="py-16 bg-white dark:bg-gray-900">
          <div className="container mx-auto px-6">
            <h2 className="text-3xl font-bold text-gray-900 dark:text-white mb-8">
              Contact
            </h2>
            <Contact formData={{name: "", email: "", message: ""}} recaptchaSiteKey="{{contact.recaptchaSiteKey}}" />
          </div>
        </section>
      </main>

      <Footer name="{{app.name}}" description="Showcase your work and skills with a modern, responsive portfolio" />
    </div>
  );
}