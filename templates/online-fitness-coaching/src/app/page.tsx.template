import Header from "@/components/layout/Header";
import Footer from "@/components/shared/Footer";
import WorkoutPlanCard from "@/components/fitness/WorkoutPlanCard";
import CoachProfile from "@/components/fitness/CoachProfile";
import ProgressTracker from "@/components/fitness/ProgressTracker";
import NutritionTracker from "@/components/fitness/NutritionTracker";
import VideoLibrary from "@/components/fitness/VideoLibrary";
import CommunityForum from "@/components/fitness/CommunityForum";
import data from "@/data/online-fitness-coaching.json";

export default function HomePage() {
  return (
    <div className="min-h-screen bg-white dark:bg-gray-950">
      <Header />

      <main>
        <section id="overview" className="bg-gradient-to-br from-[var(--color-primary)]/15 via-transparent to-[var(--color-secondary)]/15">
          <div className="container mx-auto px-6 pt-28 pb-16 md:pt-36">
            <div className="flex flex-wrap items-end justify-between gap-6">
              <div>
                <p className="text-sm font-semibold text-[var(--color-primary)]">
                  FitCoach · Progress Tracker
                </p>
                <h1 className="mt-2 text-3xl font-bold text-gray-900 dark:text-white sm:text-4xl">
                  {{app.name}}
                </h1>
                <p className="mt-3 max-w-xl text-gray-500 dark:text-gray-400">
                  {{app.tagline}}
                </p>
              </div>

              <div className="w-full max-w-xs rounded-3xl border border-gray-200 dark:border-gray-800 bg-white/70 p-5 backdrop-blur dark:bg-gray-950/70">
                <div className="mb-2 flex items-center justify-between text-sm">
                  <span className="text-gray-500 dark:text-gray-400">Progress</span>
                  <span className="font-bold text-gray-900 dark:text-white tabular-nums">
                    {data.currentStreak} day{{ data.currentStreak !== 1 ? "s" : "" }} streak
                  </span>
                </div>
                <div
                  className="h-2.5 w-full overflow-hidden rounded-full bg-gray-100 dark:bg-gray-800"
                  role="progressbar"
                  aria-label="Progress streak"
                  aria-valuenow={data.currentStreak}
                  aria-valuemin={0}
                  aria-valuemax={30}
                >
                  <div
                    className="h-full rounded-full bg-gradient-to-r from-[var(--color-primary)] to-[var(--color-secondary)]"
                    style={{ width: `${data.currentStreak / 30 * 100}%` }}
                  />
                </div>
              </div>
            </div>
          </div>
        </section>

        <section id="workout-plan" className="container mx-auto scroll-mt-24 px-6 py-20">
          <div className="mb-10 max-w-2xl">
            <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Workout Plan</h2>
            <p className="mt-2 text-gray-500 dark:text-gray-400">
              Personalized workout plans tailored to your goals and fitness level.
            </p>
          </div>
          <WorkoutPlanCard workouts={data.workouts as any} currency={data.currency as string} />
        </section>

        <section id="coach" className="bg-white dark:bg-gray-900 scroll-mt-24">
          <div className="container mx-auto px-6 py-20">
            <div className="mb-10 max-w-2xl">
              <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Certified Coaches</h2>
              <p className="mt-2 text-gray-500 dark:text-gray-400">
                Meet our certified trainers who will guide your fitness journey.
              </p>
            </div>
            <CoachProfile coach={data.coach as any} onBook={() => {}} />
          </div>
        </section>

        <section id="progress" className="scroll-mt-24 bg-gray-50 dark:bg-gray-950/40">
          <div className="container mx-auto px-6 py-20">
            <div className="mb-10 max-w-2xl">
              <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Progress Tracker</h2>
              <p className="mt-2 text-gray-500 dark:text-gray-400">
                Visualize your achievements in weight loss or muscle gain over time.
              </p>
            </div>
            <ProgressTracker progress={data.progress as any} goal="reach target weight" />
          </div>
        </section>

        <section id="nutrition" className="bg-white dark:bg-gray-900 scroll-mt-24">
          <div className="container mx-auto px-6 py-20">
            <div className="mb-10 max-w-2xl">
              <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Nutrition Tracker</h2>
              <p className="mt-2 text-gray-500 dark:text-gray-400">
                Log meals, track calories, and receive dietary suggestions.
              </p>
            </div>
            <NutritionTracker meals={data.mealLogs as any} dailyCalorieGoal={data.dailyCalorieGoal as any} />
          </div>
        </section>

        <section id="video-library" className="scroll-mt-24 bg-gray-50 dark:bg-gray-950/40">
          <div className="container mx-auto px-6 py-20">
            <div className="mb-10 max-w-2xl">
              <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Video Library</h2>
              <p className="mt-2 text-gray-500 dark:text-gray-400">
                Exercise demonstrations across all fitness levels.
              </p>
            </div>
            <VideoLibrary videos={data.videos as any} categories={data.videoCategories as any} />
          </div>
        </section>

        <section id="community" className="bg-gray-50 dark:bg-gray-950/40 scroll-mt-24">
          <div className="container mx-auto px-6 py-20">
            <div className="mb-10 max-w-2xl">
              <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Community Forum</h2>
              <p className="mt-2 text-gray-500 dark:text-gray-400">
                Share experiences, ask questions, and support others in their fitness journeys.
              </p>
            </div>
            <CommunityForum posts={data.forumPosts as any} />
          </div>
        </section>
      </main>

      <Footer name={data.appName as string} description={data.tagline as string} />
    </div>
  );
}