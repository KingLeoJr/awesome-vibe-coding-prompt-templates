import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";
import ActivityRing from "@/components/fitness/ActivityRing";
import WeeklyOverview from "@/components/fitness/WeeklyOverview";
import WorkoutLog from "@/components/fitness/WorkoutLog";
import NutritionTracker from "@/components/fitness/NutritionTracker";
import AchievementBadge from "@/components/fitness/AchievementBadge";
import data from "@/data/fitness-tracking.json";

export default function HomePage() {
  const today = new Date().toLocaleDateString(undefined, {
    weekday: "long",
    month: "long",
    day: "numeric",
  });

  return (
    <div className="min-h-screen bg-gray-50 dark:bg-gray-950">
      <Header />

      <main>
        <section
          id="overview"
          className="bg-gradient-to-br from-[var(--color-primary)]/10 via-transparent to-[var(--color-secondary)]/10"
        >
          <div className="container mx-auto px-6 pt-28 pb-16 md:pt-36">
            <p className="text-sm font-semibold text-[var(--color-primary)]">{today}</p>
            <h1 className="mt-2 text-3xl font-bold text-gray-900 dark:text-white sm:text-4xl">
              Welcome back, {data.user.name.split(" ")[0]}
            </h1>
            <p className="mt-3 max-w-xl text-gray-500 dark:text-gray-400">
              {data.user.goal}. &ldquo;{data.quotes[0]}&rdquo;
            </p>

            <div className="mt-10 rounded-3xl border border-gray-200 dark:border-gray-800 bg-white/60 p-8 backdrop-blur dark:bg-gray-950/60">
              <ActivityRing rings={data.rings} />
            </div>
          </div>
        </section>

        <section id="activity" className="container mx-auto scroll-mt-24 px-6 py-20">
          <div className="mb-10 max-w-2xl">
            <h2 className="text-3xl font-bold text-gray-900 dark:text-white">This week</h2>
            <p className="mt-2 text-gray-500 dark:text-gray-400">
              Active minutes, calories burned, and your personal goals at a glance.
            </p>
          </div>
          <WeeklyOverview weekly={data.weekly} goals={data.goals} />
        </section>

        <section id="workouts" className="bg-white dark:bg-gray-900 scroll-mt-24">
          <div className="container mx-auto px-6 py-20">
            <div className="mb-10 max-w-2xl">
              <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Workouts</h2>
              <p className="mt-2 text-gray-500 dark:text-gray-400">
                Log exercises, sets, reps, and weights — every rep counts.
              </p>
            </div>
            <WorkoutLog workouts={data.workouts} />
          </div>
        </section>

        <section id="nutrition" className="container mx-auto scroll-mt-24 px-6 py-20">
          <div className="mb-10 max-w-2xl">
            <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Nutrition</h2>
            <p className="mt-2 text-gray-500 dark:text-gray-400">
              Hit your calorie and macro targets with meals that fit your plan.
            </p>
          </div>
          <NutritionTracker nutrition={data.nutrition} meals={data.meals} />
        </section>

        <section id="goals" className="bg-white dark:bg-gray-900 scroll-mt-24">
          <div className="container mx-auto px-6 py-20">
            <div className="mb-10 max-w-2xl">
              <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Badges &amp; challenges</h2>
              <p className="mt-2 text-gray-500 dark:text-gray-400">
                Little wins, earned daily — keep the streak alive.
              </p>
            </div>
            <AchievementBadge badges={data.badges} challenges={data.challenges} />
          </div>
        </section>
      </main>

      <Footer />
    </div>
  );
}
