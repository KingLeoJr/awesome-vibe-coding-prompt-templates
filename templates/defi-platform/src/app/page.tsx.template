import { motion } from "framer-motion";
import {
  ArrowRight,
  Banknote,
  Hexagon,
  Layers,
  Scale,
  TrendingUp,
  Users,
  Vote,
  Wallet,
} from "lucide-react";
import SEOHead from "@/components/shared/SEOHead";
import Header from "@/components/layout/Header";
import Footer from "@/components/shared/Footer";
import WalletConnect from "@/components/defi/WalletConnect";
import SwapInterface from "@/components/defi/SwapInterface";
import LiquidityPool from "@/components/defi/LiquidityPool";
import YieldFarm from "@/components/defi/YieldFarm";
import data from "@/data/{{app.slug}}.json";

const heroStats = [
  { icon: Layers, label: "Total value locked", key: "tvl" },
  { icon: TrendingUp, label: "24h volume", key: "volume24h" },
  { icon: Wallet, label: "Active wallets", key: "wallets" },
  { icon: Scale, label: "Total farms", key: "farms" },
];

const sectionHeading = (id: string, title: string, subtitle: string) => (
  <div className="mx-auto mb-10 max-w-2xl text-center">
    <h2 id={id} className="text-3xl font-bold tracking-tight text-gray-900 dark:text-white sm:text-4xl">
      {title}
    </h2>
    <p className="mt-3 text-base text-gray-500 dark:text-gray-400">{subtitle}</p>
  </div>
);

export default function Home() {
  return (
    <>
      <SEOHead />
      <Header />
      <main>
        <section id="overview" className="relative overflow-hidden pb-24 pt-36">
          <div
            aria-hidden="true"
            className="pointer-events-none absolute inset-0 bg-[radial-gradient(ellipse_at_top,color-mix(in_srgb,var(--color-primary)_12%,transparent),transparent_60%)]"
          />
          <div className="container mx-auto px-6 text-center">
            <motion.p
              initial={{ opacity: 0, y: 12 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.5 }}
              className="mx-auto inline-flex items-center gap-2 rounded-full border border-[var(--color-primary)]/30 bg-[var(--color-primary)]/5 px-4 py-1.5 text-xs font-medium text-[var(--color-primary)]"
            >
              <Hexagon size={13} />
              {data.app.tagline}
            </motion.p>
            <motion.h1
              initial={{ opacity: 0, y: 16 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6, delay: 0.1 }}
              className="mx-auto mt-6 max-w-3xl text-4xl font-bold tracking-tight text-gray-900 dark:text-white sm:text-6xl"
            >
              DeFi that{" "}
              <span className="bg-gradient-to-r from-[var(--color-primary)] to-[var(--color-secondary)] bg-clip-text text-transparent">
                works for everyone.
              </span>
            </motion.h1>
            <motion.p
              initial={{ opacity: 0, y: 16 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6, delay: 0.2 }}
              className="mx-auto mt-6 max-w-2xl text-lg text-gray-500 dark:text-gray-400"
            >
              Swap at oracle-verified prices, provide liquidity, farm yield, and
              vote on governance — all from a sleek, gas-aware interface.
            </motion.p>
            <motion.div
              initial={{ opacity: 0, y: 16 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6, delay: 0.3 }}
              className="mt-8 flex flex-wrap items-center justify-center gap-4"
            >
              <a
                href="#swap"
                className="inline-flex items-center gap-2 rounded-full bg-[var(--color-primary)] px-7 py-3 text-sm font-medium text-white transition hover:opacity-90"
              >
                Start swapping <ArrowRight size={16} />
              </a>
              <a
                href="#pools"
                className="inline-flex items-center gap-2 rounded-full border border-gray-300 px-7 py-3 text-sm font-medium text-gray-700 transition hover:bg-gray-50 dark:border-gray-700 dark:text-gray-200 dark:hover:bg-gray-900"
              >
                <Banknote size={16} />
                Earn yield
              </a>
            </motion.div>
          </div>
        </section>

        <section className="container mx-auto -mt-12 px-6">
          <div className="grid grid-cols-2 gap-4 lg:grid-cols-4">
            {data.stats.map((stat, index) => {
              const Icon = heroStats[index]?.icon ?? TrendingUp;
              return (
                <motion.div
                  key={stat.label}
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ duration: 0.45, delay: index * 0.07 }}
                  className="rounded-2xl border border-gray-200 bg-white p-5 shadow-sm dark:border-gray-800 dark:bg-gray-950"
                >
                  <span className="flex h-9 w-9 items-center justify-center rounded-xl bg-[var(--color-primary)]/10 text-[var(--color-primary)]">
                    <Icon size={18} />
                  </span>
                  <p className="mt-3 text-2xl font-bold text-gray-900 dark:text-white">{stat.value}</p>
                  <p className="text-xs text-gray-500 dark:text-gray-400">
                    {stat.label} · <span className="text-[var(--color-accent)]">{stat.delta}</span>
                  </p>
                </motion.div>
              );
            })}
          </div>
        </section>

        <section id="swap" className="py-24">
          <div className="container mx-auto grid gap-10 px-6 lg:grid-cols-5 lg:items-center">
            <div className="lg:col-span-2">
              {sectionHeading("swap", "Swap in seconds", "Chainlink-priced token exchange with slippage control and gas optimization.")}
              <ul className="space-y-3 text-sm text-gray-600 dark:text-gray-300">
                <li className="flex items-start gap-2.5">
                  <TrendingUp size={18} className="mt-0.5 shrink-0 text-[var(--color-primary)]" />
                  Real-time prices from {{chain.network}} oracle feeds.
                </li>
                <li className="flex items-start gap-2.5">
                  <Users size={18} className="mt-0.5 shrink-0 text-[var(--color-primary)]" />
                  Deep liquidity across AMM pools with optimal routing.
                </li>
                <li className="flex items-start gap-2.5">
                  <Vote size={18} className="mt-0.5 shrink-0 text-[var(--color-primary)]" />
                  EIP-1559 aware fees — no overpaying for gas.
                </li>
              </ul>
            </div>
            <div className="lg:col-span-3">
              <div className="grid gap-6 lg:grid-cols-2 lg:items-start">
                <SwapInterface tokens={data.tokens} />
                <WalletConnect />
              </div>
            </div>
          </div>
        </section>

        <section id="pools" className="bg-gray-50 py-24 dark:bg-gray-900/40">
          <div className="container mx-auto px-6">
            {sectionHeading("pools", "Liquidity pools", "Provide liquidity to automated market makers and earn fees plus rewards.")}
            <LiquidityPool pools={data.pools} />
          </div>
        </section>

        <section id="farms" className="py-24">
          <div className="container mx-auto px-6">
            {sectionHeading("farms", "Yield farms", "Stake LP tokens or single assets to farm emissions with real-time APY.")}
            <YieldFarm farms={data.farms} />
          </div>
        </section>

        <section id="governance" className="bg-gray-50 py-24 dark:bg-gray-900/40">
          <div className="container mx-auto px-6">
            {sectionHeading("governance", "On-chain governance", "Propose, debate, and vote — the protocol is governed by its community.")}
            <div className="mx-auto max-w-3xl space-y-4">
              {data.proposals.map((proposal, index) => {
                const total = proposal.forVotes + proposal.againstVotes;
                const forPct = total > 0 ? (proposal.forVotes / total) * 100 : 0;
                return (
                  <motion.article
                    key={proposal.id}
                    initial={{ opacity: 0, y: 20 }}
                    whileInView={{ opacity: 1, y: 0 }}
                    viewport={{ once: true }}
                    transition={{ duration: 0.45, delay: index * 0.07 }}
                    className="rounded-2xl border border-gray-200 bg-white p-6 dark:border-gray-800 dark:bg-gray-950"
                  >
                    <div className="flex flex-wrap items-center justify-between gap-3">
                      <h3 className="font-semibold text-gray-900 dark:text-white">{proposal.title}</h3>
                      <span className="inline-flex items-center gap-1 rounded-full bg-[var(--color-primary)]/10 px-2.5 py-1 text-xs font-medium text-[var(--color-primary)]">
                        {proposal.status}
                      </span>
                    </div>
                    <div className="mt-4">
                      <div className="mb-1.5 flex justify-between text-xs text-gray-500 dark:text-gray-400">
                        <span>{proposal.forVotes.toLocaleString()} for</span>
                        <span>{proposal.againstVotes.toLocaleString()} against</span>
                      </div>
                      <div className="flex h-2 w-full overflow-hidden rounded-full bg-gray-100 dark:bg-gray-800" role="progressbar" aria-label="Vote tally" aria-valuenow={forPct} aria-valuemin={0} aria-valuemax={100}>
                        <motion.div
                          initial={{ width: 0 }}
                          whileInView={{ width: `${forPct}%` }}
                          viewport={{ once: true }}
                          transition={{ duration: 0.9, ease: "easeOut" }}
                          className="h-full rounded-full bg-[var(--color-accent)]"
                        />
                      </div>
                      <p className="mt-3 text-xs text-gray-400">
                        Quorum {proposal.quorum.toLocaleString()} votes · closes {proposal.deadline}
                      </p>
                    </div>
                  </motion.article>
                );
              })}
            </div>
          </div>
        </section>

        <section className="py-24">
          <div className="container mx-auto px-6">
            <div className="relative overflow-hidden rounded-3xl border border-[var(--color-primary)]/20 bg-gradient-to-br from-[var(--color-primary)] to-[var(--color-secondary)] px-8 py-14 text-center">
              <div className="relative z-10">
                <h2 className="text-3xl font-bold text-white sm:text-4xl">Ready to take control?</h2>
                <p className="mx-auto mt-4 max-w-xl text-sm text-white/80">
                  Connect your wallet and start swapping, farming, and governing on {{chain.network}}.
                </p>
                <a
                  href="#swap"
                  className="mt-8 inline-flex items-center gap-2 rounded-full bg-white px-7 py-3 text-sm font-medium text-[var(--color-primary)] transition hover:bg-gray-100"
                >
                  Connect wallet <ArrowRight size={16} />
                </a>
              </div>
            </div>
          </div>
        </section>
      </main>
      <Footer />
    </>
  );
}