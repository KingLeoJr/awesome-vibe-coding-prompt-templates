import Header from "@/components/layout/Header";
import Footer from "@/components/shared/Footer";
import PetCard from "@/components/pet/PetCard";
import ServiceProvider from "@/components/pet/ServiceProvider";
import BookingSystem from "@/components/pet/BookingSystem";
import VaccinationTracker from "@/components/pet/VaccinationTracker";
import ResourceLibrary from "@/components/pet/ResourceLibrary";
import CostEstimator from "@/components/pet/CostEstimator";
import data from "@/data/pet-care-platform.json";

export default function HomePage() {
  return (
    <div className="min-h-screen bg-white dark:bg-gray-950">
      <Header />

      <main>
        <section id="overview" className="bg-gradient-to-br from-[var(--color-primary)]/15 via-transparent to-[var(--color-secondary)]/15">
          <div className="container mx-auto px-6 pt-28 pb-16 md:pt-36">
            <div className="flex flex-wrap items-end justify-between gap-6">
              <div>
                <p className="text-sm font-semibold text-[var(--color-primary)]">
                  PetCare · Community Hub
                </p>
                <h1 className="mt-2 text-3xl font-bold text-gray-900 dark:text-white sm:text-4xl">
                  {{app.name}}
                </h1>
                <p className="mt-3 max-w-xl text-gray-500 dark:text-gray-400">
                  {{app.tagline}}
                </p>
              </div>

              <div className="w-full max-w-xs rounded-3xl border border-gray-200 dark:border-gray-800 bg-white/70 p-5 backdrop-blur dark:bg-gray-950/70">
                <div className="mb-2 flex items-center justify-between text-sm">
                  <span className="text-gray-500 dark:text-gray-400">Pets cared for</span>
                  <span className="font-bold text-gray-900 dark:text-white tabular-nums">
                    {data.totalPets}
                  </span>
                </div>
                <div
                  className="h-2.5 w-full overflow-hidden rounded-full bg-gray-100 dark:bg-gray-800"
                  role="progressbar"
                  aria-label="Pets cared for"
                  aria-valuenow={data.totalPets}
                  aria-valuemin={0}
                  aria-valuemax={100}
                >
                  <div
                    className="h-full rounded-full bg-gradient-to-r from-[var(--color-primary)] to-[var(--color-secondary)]"
                    style={{ width: `${data.adoptionProgress}%` }}
                  />
                </div>
              </div>
            </div>
          </div>
        </section>

        <section id="providers" className="container mx-auto scroll-mt-24 px-6 py-20">
          <div className="mb-10 max-w-2xl">
            <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Service Providers</h2>
            <p className="mt-2 text-gray-500 dark:text-gray-400">
              Find vetted pet service providers near you — groomers, trainers, and veterinarians.
            </p>
          </div>
          <ServiceProvider providers={data.providers as any} category="all" />
        </section>

        <section id="booking" className="bg-white dark:bg-gray-900 scroll-mt-24">
          <div className="container mx-auto px-6 py-20">
            <div className="mb-10 max-w-2xl">
              <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Book an Appointment</h2>
              <p className="mt-2 text-gray-500 dark:text-gray-400">
                Schedule a session with a local pet service provider based on availability.
              </p>
            </div>
            <BookingSystem provider={data.defaultProvider as any} onBook={() => {}} />
          </div>
        </section>

        <section id="health" className="scroll-mt-24 bg-gray-50 dark:bg-gray-950/40">
          <div className="container mx-auto px-6 py-20">
            <div className="mb-10 max-w-2xl">
              <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Health Tracker</h2>
              <p className="mt-2 text-gray-500 dark:text-gray-400">
                Log vaccinations, track health metrics, and monitor your pet's well-being.
              </p>
            </div>
            <VaccinationTracker records={data.healthRecords as any} />
          </div>
        </section>

        <section id="resources" className="bg-gray-50 dark:bg-gray-950/40 scroll-mt-24">
          <div className="container mx-auto px-6 py-20">
            <div className="mb-10 max-w-2xl">
              <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Resource Library</h2>
              <p className="mt-2 text-gray-500 dark:text-gray-400">
                Articles, videos, and guides on pet care topics — nutrition, training, and health tips.
              </p>
            </div>
            <ResourceLibrary articles={data.articles as any} />
          </div>
        </section>

        <section id="cost" className="bg-white dark:bg-gray-900 scroll-mt-24">
          <div className="container mx-auto px-6 py-20">
            <div className="mb-10 max-w-2xl">
              <h2 className="text-3xl font-bold text-gray-900 dark:text-white">Cost Estimator</h2>
              <p className="mt-2 text-gray-500 dark:text-gray-400">
                Estimate costs for pet services based on type and your budget.
              </p>
            </div>
            <CostEstimator serviceType="grooming" budget={data.defaultBudget as any} />
          </div>
        </section>
      </main>

      <Footer name={data.appName as string} description={data.tagline as string} />
    </div>
  );
}